# Copy to .env (gitignored — never commit real credentials) and fill in.
# Local dev mirror only — see CLAUDE.md's "Local development mirror" section.
SCOOP_BASE_URL=https://ops.swanky.local
SCOOP_TEST_USER=
SCOOP_TEST_PASS=
